import React from 'react';
import Header from '../../layout/Header';

const AboutPage = () => {
    return (
        <div>
            <Header/>
            <h1>About us</h1>
        </div>
    );
};

export default AboutPage; // Не забываем экспортировать!